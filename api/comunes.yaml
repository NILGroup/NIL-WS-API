components:
  parameters:
    PalabraEnUrl:
      name: palabra
      description: una palabra en castellano
      in: path
      required: true
      schema:
        type: string
  schemas:
    TextoEnBodyJSON:
      required: true
      content:
        application/json:
          schema:
            type: object
            required:
              - texto
            properties:
              texto:
                type: string
